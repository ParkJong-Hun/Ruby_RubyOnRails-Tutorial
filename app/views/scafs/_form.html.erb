<%= form_with(model: scaf) do |form| %>
  <% if scaf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scaf.errors.count, "error") %> prohibited this scaf from being saved:</h2>

      <ul>
        <% scaf.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
